import React, { Component } from 'react';
import WebMap from './WebMap';

const WebMapBlockEdit = () => {
    return (
        <div>
            <WebMap />
        </div>
    );
}

export default WebMapBlockEdit;